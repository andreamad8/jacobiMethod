/* We tried the following business logic
*/
float sum = 0.0;
for (size_t j = 0; j < N; j++) {
  sum = sum + R[i][j] * x1[j];
}
x2[i] = (b[i] - sum) / D[i];
/// SER

/// THREAD
// PHI
// XEON

/// FASTFLOW
// PHI
// XEON
